# 粵學堂 · P0/P1 修复报告（2026-08-29）

> 依据复检清单逐条核实后改码。**全部改动未提交**（原因见文末"提交前必读"）。
> 基线：HEAD `24a01d3`（比你报告的 `7d38dd0` 更新，已含粤拼拼写练习）。

---

## 一、P0 修复

### P0-1 主题课答错卡死 + 粤拼泄露 ✅
**实际位置**：`js/app.js` 主题课 `tones` 步骤（约 1440-1530 行），不是 1325（1325 是 `d5Finish`）。

| 问题 | 修复 |
|---|---|
| 答错后两按钮 `disabled`，且只在答对时才 `setTimeout` 推进 → 无重试入口，只能关弹窗重进 | 第 1 次答错**保留两个选项**重选（仅闪一下错误态）；第 2 次才公布答案并显示「继续 →」 |
| 听音按钮直接渲染 `pair.a.jp`，`si1/si4` 提前泄露 | 粤拼移入 `#ltJpA/#ltJpB` 空占位，**作答后**才 `revealJp()` 揭示 |
| — | 新增「🔁 两个都听一次」；答对后定时器加 `if(!$('#ltPickA')) return;`，避免用户已关窗又被自动打开 |

### P0-2 课程/阅读进度未进云端 ✅
服务端 `sanitizeProfile` 补上两个字段，并新增按条目合并：
- `lessonProgress`：slug 校验 `/^[a-z0-9][a-z0-9_-]{0,63}$/`
- `stories`：字符串列表去重，上限 50
- 新增 `mergeLessonProgress()`（已完成优先，其次 step 大者胜）、`mergeStories()`（并集），`sync-server.mjs` PUT 时调用

### P0-3 SRS 多设备合并 ✅（根因比预估多一层）
1. **客户端**：`normalizeProgress` 每次保存都把 `updatedAt` 抹掉 → 已保留，并补 `due` 日期格式校验（`bad-date` 会让服务端整条丢弃）。
2. **客户端（你未提到的一半）**：`mergeCloud3` 对 `reviews`/`lessonProgress`/`stories` 走 else 分支**整包覆盖**；且本地未改动时 `out[key]=remote[key]`，云端缺字段就会清空本地。
   → 新增 `mergeReviews3`（按词取 updatedAt 较新者）、`mergeLesson3`、`mergeStories3`，在循环顶部 `continue`；`mergeFirstCloud` 同样补上。
3. **服务端** `mergeReviews` 本就正确，之前只是收不到时间戳。

已隔离验证：远端字段缺失 / 为空时，本地数据均不再被清空。

### P0-4 自动备份 ✅
| 问题 | 修复 |
|---|---|
| `User=jyut` 不存在 | → `jyut-sync`（与 `jyut-sync.service` 一致，才能读 `/var/lib/jyut-sync/store.json`） |
| `ExecStart=/opt/jyut/server/...` 路径不符 | → `/opt/jyut-sync-live/backup-store.mjs`（+ `/usr/local/bin/node`） |
| 未设 `JYUT_SYNC_DATA_FILE` | 补上——`backup-store.mjs` 复用 `sync-store.mjs` 的 `DATA_FILE`，不设会去读 cwd 下的默认路径，**备份的是错文件** |
| 加密清理漏 `.enc` | `prune` 同时匹配 `.json` 与 `.json.enc` |
| 密钥写在 unit 里 | 改为可选 `EnvironmentFile=/etc/jyut-backup.env`（默认注释），密钥不暴露给非特权用户 |
| **unit 从未安装** | 部署脚本新增 `install_backup_units()`：install 两个 unit → daemon-reload → `enable --now` → 校验 active；并先确认 `backup-store.mjs` 已发布到位 |

> **比清单更严重的一点**：`deploy-jyut-clean.sh` 的 install 清单里**从来没有 `backup-store.mjs`**——即使手工装了 unit，ExecStart 也会因文件不存在而失败。已补进必需文件清单、`node --check`、install 进 `SYNC_STAGE`、并纳入 `SYNC_CHANGED` 比较。

---

## 二、P1 修复

### P1-1 离线与 CI ✅
- `sw.js` CORE 补 `js/lessons.js`、`js/stories.js`
- `ci.yml` 语法检查补两个新文件 + `bash -n deploy/deploy-jyut-clean.sh`
- `verify-data.mjs` 缓存一致性资产清单补两个新文件
- **执行了 `node scripts/bump-version.mjs`（→ v28）**：改动缓存清单后必须升版本，否则已安装 PWA 仍用旧缓存，离线照样失败

### P1-2 语料 + 校验器 ✅

**先说一条误报**：`拒絕` 的 `keoi5 keoi5 zyut6` **不是重复**——`佢=keoi5`、`拒=keoi5` 是真实同音字，7 汉字 / 7 音节完全对齐。改了反而破坏注音，未动。

真问题已修：
| 位置 | 问题 | 处理 |
|---|---|---|
| `銀紙` | `jp:' ngan4 zi2'` 前导空格 | 去除 |
| `行人路` | 粤拼对齐正确，但**语义**错：粤语「行馬路」=在马路上走，释义却写「**过**马路」→「在马路上走，要走人行道」自相矛盾 | 改「唔好行馬路，要行行人路／不要走马路，要走人行道」 |

新发现并修复：
- `檸茶`(2字) 标了 `ning4 mung4 caa4`(3音节) → 词头改 `檸檬茶`，例句同步
- `菠蘿油` 例句 `ning2` 笔误 → `ning4`（全库 `ning2` 仅 1 处、`ning4` 两处）
- **故事 `newWords` 里的 `侍應` 不在词库** → 读完静默跳过，永远进不了复习。已补入 food 类目（300→**331 词**）；注音取标准 `si6 jing3`，故事原为 `si6 jing1`，**此项待人工确认**
- **课程 `lifeTitle: '「一盅兩件」嘆世界'` 与 data.js 实际 `'「一盅两件」叹世界'` 不匹配** → 静默回退 `culture.life[0]`，导致饮茶课显示成「茶餐厅：丝袜奶茶与暗语」。已按实际数据修正

**校验器扩充**（`scripts/verify-data.mjs`）：
- 首尾空格、词头/例句注音对齐（剥离 `（）` 注释、计入拉丁字母——「唱K」的 K 读 kei1）
- 课程引用完整性（`catId`/`words`/`dlgId`/`artId`/`lifeTitle`）、故事生词必须在词库、理解题答案不越界、id 唯一
- 简繁一致性：词汇/对话/故事/课程 **强校验**；语法标题（8 处）与文化标题（9/14）**仅警告**

**简繁统一**：`stories.js` 全量转繁体（正文/题干/选项），`lessons.js` 展示文本转繁体；`culture.life` 标题是 `lifeTitle` 查表键，本轮不动，列入警告待人工校对。

---

## 三、验证结果

```
✅ 语法检查：9 个 JS 文件 + deploy 脚本全部通过
✅ verify-data：词汇 331 · 歌词 138 句 · 主题课 1 · 故事 1
✅ server/sync-store.test.mjs：12 项全过（原 6 项 + 新增 6 项）
✅ 客户端合并函数隔离验证：含「远端缺字段不清空本地」回归
⚠ 17 条警告（语法/文化标题简体，不阻塞）
```

---

## 四、提交前必读：工作区有并发未提交改动

`git status` 显示 `README.md`、`css/style.css`、`index.html`、`js/app.js` 存在**并非本次修改**的改动——粤拼拼写练习正在新增 `difficulty` 难度分级与 `parseJyutping()`。

**因此本次所有改动均未提交**，避免夹带他人 WIP。提交前请确认这些并发改动是否要一起带上。

版本号已统一到 `v28`（sw.js CACHE / CORE 数组 / index.html 三处一致）。

---

## 五、未处理（按你的建议保留）

- 补 2–3 节主题课（搭巴士 / 街市买餸 / 睇医生）+ 2–3 篇配套阅读
- 课程完成标记「已接触」，经几轮 SRS 后才升级为「已掌握」
- 词汇每类 30 词，手机端默认「今日 5 词 / 学习中 / 查看全部」
- 阅读全文播放加当前句高亮；粤拼与普通话释义分别切换
- 声调易错记录目前仅本机，需轻量同步
- **版权**：11 首完整歌词建议改学习片段 / 取得授权 / 外链正版来源
- 发音人工校对清单：`侍應 si6 jing3`（原 si6 jing1）、`檸` 统一 ning4
