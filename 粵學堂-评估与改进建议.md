# 粵學堂 · 项目评估与改进建议

> 评估对象：当前工作区 `Cantonese/` 静态 PWA（HTML + 原生 JS + 自建 Node 后端）
> 评估视角：功能完整性、学习闭环有效性、PWA/平台能力、代码与内容一致性
> 结论：**架构与完成度已经相当成熟，是一个“可用且用心”的产品；主要提升空间在「学习闭环的有效性」与「离线能力兑现」，而非堆功能。**

---

## 1. 总体结论（一句话）

粤语学习的"听—读—练—唱—文化"主干已经具备，工程质量（PWA、语音双通道、云端同步三路合并、安全）在同体量个人项目里属于上乘；**但作为语言学习产品，它现在更像一个"带语音的电子课本"，缺少"练得准、记得牢"的闭环**——发音反馈是节奏而非准确度、没有声调听辨训练、没有间隔复习。这是最值得投入的方向。

---

## 2. 已经做得很好的地方（先点赞，再挑刺）

| 维度 | 具体表现 |
|------|----------|
| **语音架构** | 云端 TTS（晓佳神经音色）为主、设备粤语语音兜底、**明确拒绝用普通话冒充粤语教学音**——这是非常正确且少见的取舍，README/诊断面板都讲清楚了。 |
| **录音反馈的诚实性** | `evalRecord()` 只评估时长+音量并明确标注"不识别发音准确性"，UI 里有 `feedback-boundary` 提示。没有用假分数误导用户。 |
| **云端同步** | `mergeCloud3` 三方合并 + 乐观并发 + `HttpOnly+Secure+SameSite` 会话 + scrypt 密码，边界清晰（录音/密码永不上云）。安全水位很高。 |
| **PWA 策略** | `sw.js` 导航 network-first、静态 stale-while-revalidate、发布版本号机制成熟。 |
| **数据驱动** | 内容集中在 `data.js`/`songs.js`，改数据即改内容；歌词有汉字数=音节数对齐校验脚本。 |
| **可访问性基础** | 11 处 `aria-` 标注、键盘可达（收藏项、歌词行有 keydown 处理）。 |
| **内容质量** | 粤拼标注专业（入声 6+3 处理、notes 难点提示到位），歌词 ruby 注音工程量大且准确。 |

---

## 3. 提升空间（按价值排序）

### P0 · 学习闭环有效性（最核心缺口）

**3.1 声调听辨训练（Tone Discrimination）完全缺失**
粤语最难的是 6/9 个声调的听辨与模仿，但全站没有任何"听一个音→选声调"的交互练习。`DATA.tones` 已就绪，却只用于"看卡片"。这是普通话母语者最痛的点，优先级最高。

**3.2 录音反馈是"节奏分"而非"准确度"**
`analyzeBlob()` 已把录音解码成 AudioBuffer（app.js:408），却只算了时长与 RMS。既然已拿到波形，完全可以做**基频（F0）轮廓提取**，把用户声调曲线和该字的标准调值（55/35/33/21/13/22/入声 1·3·6）对比，把现在的"手动声调自查"升级成**自动化声调反馈**。纯前端、零新依赖、与现有 `renderFeedback` 直接对接。

**3.3 没有间隔重复（SRS）**
收藏=扁平列表，学过即算 `learned`（点卡片就记）。缺少遗忘曲线驱动的复习队列， retention（ retention）靠自觉。应在 `freshProgress()` 增加 `due` 字段，做轻量 SM-2。

**3.4 听力小测太浅**
`openQuiz()` 只做"听单词→选普通话意思"（app.js:755）。缺少：句子听写（听例句→选正确汉字）、声调选择题、最小对立词（minimal pairs，如 詩/史/試/時 si1·si2·si3·si4）辨析。

### P1 · 平台 / PWA 兑现

**3.5 "离线可用"名不副实**
manifest/PWA 声称离线可用，但 `sw.js` 只缓存静态资源，**TTS 音频没缓存**。没装设备粤语语音包时，离线打开语音/唱歌/对话页全部哑火。建议把生成的 mp3 按 `text|voice|rate` 缓存进 Cache Storage / IndexedDB，使"复习已学内容"真正离线可用。

**3.6 iOS 长文本 TTS 截断**
iOS `speechSynthesis` 对长句会中途截断。现有 `speakLocal` 未分块；可对 >15 字文本按标点切分顺序朗读。

**3.7 录音无实时反馈**
练习台只有状态文字，没有音量/VU 电平条或波形。加一个 `AnalyserNode` 实时电平，能显著降低"不知道录没录上"的焦虑。

### P2 · 内容规模与互动

**3.8 内容规模 vs "进阶"定位有落差**
实测：词汇 **165** 词 / **11** 场景（README 写 153/10）、对话 **9** 个（README 写 6）、语法 10、文化 58、歌曲 11（138 句）。对"零到进阶"，词汇量偏小，建议补：职场/网购/求医深度/问路扩展等场景；对话从 9 扩到 12+。

**3.9 语法与文化是"只读"**
语法 10 讲只有阅读+例句播放；文化 58 条只能听。可加：语法填空/选择题、俗语"猜意思"小互动。

**3.10 唱歌模块无"逐句评分留存"**
`singRec` 录完即弃，不进复习队列，与 SRS 打通即可。

### P3 · 代码与一致性（维护类）

**3.11 README 数据漂移（应顺手修）**
实测与文档不一致：

| 项 | README 声称 | 代码实测 |
|----|------------|----------|
| 韵母 | 56 | **55**（`DATA.finals.length`）|
| 场景数 | 10 大场景 | **11** |
| 词汇数 | 153 | **165** |
| 对话场景 | 6 | **9** |

首页 `index.html` 已显示正确值（55/11/165/9），是 README 落后了。

**3.12 app.js 单文件 1606 行**
功能全挤在一个文件，长期维护成本高。可拆成 `router.js / tts.js / recorder.js / modules/*.js` 的 ES Module，不影响现有行为。可选。

**3.13 "learned" 语义过宽**
点击即记为 learned，建议拆为 `heard`（听过）/ `practiced`（练过）/ `mastered`（小测通过），让档案更真实。

**3.14 可访问性细节**
11 处 aria 但 modal 无 focus trap（仅 1 处 focus 调用），键盘 Tab 顺序、sidebar 键盘可达可补。

---

## 4. 可落地的修改建议清单（按优先级）

| # | 建议 | 改动点 |  effort | 价值 |
|---|------|--------|---------|------|
| 1 | **声调听辨 Drill**：播放音节→选调值，基于 `DATA.tones`+词汇最小对立集 | 新增 `renderToneQuiz()`，复用 `speak()` | S | ⭐⭐⭐ |
| 2 | **自动声调评估**：`analyzeBlob` 增 F0 轮廓 → 对标调值 → 自动反馈替代手动自查 | app.js `analyzeBlob`/`renderFeedback` | M | ⭐⭐⭐ |
| 3 | **间隔复习 SRS**：`freshProgress` 加 `due`，首页/档案加"待复习"队列 | app.js 进度模型 + 新页面 | M | ⭐⭐⭐ |
| 4 | **小测升级**：加句子听写 + 最小对立词 + 声调选择 | `openQuiz()` | S | ⭐⭐ |
| 5 | **离线 TTS 缓存**：命中即存 Cache Storage，复习离线可用 | `sw.js` + `speak()` | M | ⭐⭐ |
| 6 | **iOS 长文本分块朗读** | `speakLocal()` | S | ⭐⭐ |
| 7 | **录音实时电平条** | `initRecorder` 加 `AnalyserNode` | S | ⭐ |
| 8 | **语法/文化互动练习** | `renderGrammar`/`renderCulture` | M | ⭐ |
| 9 | **扩充词汇到 250+、对话到 12+** | `data.js` | L(内容) | ⭐⭐ |
| 10 | **修 README 数据漂移 + 拆分 app.js** | README / 重构 | S/M | ⭐ |

---

## 5. 推荐路线图

- **本周可做（低成本高回报）**：#1 声调听辨、#4 小测升级、#6 iOS 分块、#10 修文档。
- **本月重点（核心闭环）**：#2 自动声调评估、#3 SRS 复习、#5 离线 TTS。
- **长期**：#8/#9 内容互动与规模、#12 代码模块化。

> 一句话给方向：**别急着加新模块，先把"练得准（声调反馈）+ 记得牢（SRS）+ 离线也能练（TTS 缓存）"这三件事做扎实，产品力会上一整个台阶。**
