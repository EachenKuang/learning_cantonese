# 粵學堂 · 控件（UI 组件）优化清单

> 聚焦"交互控件"层面的可优化点（不含上一轮已覆盖的功能/架构）。
> 评估依据：通读 `index.html`、`css/style.css`（880 行）、`js/app.js`。
> 已确认做得好的：全局 `focus-visible`（金色描边）、`prefers-reduced-motion` 已处理、`role="dialog" aria-modal` 在部分弹层已用、`.switch` 自定义开关组件已存在。

---

## P0 — 无障碍硬伤（影响键盘 / 读屏用户，建议优先修）

### 1. 模态框没有焦点陷阱，也不支持 Esc 关闭，背景未 `inert`
**现状**
- 弹层有 4+ 类：听力小测 `qMask`（app.js:999）、跟读反馈 `evalClose`（:1162）、跟唱反馈（:1700）、全文注音 `flMask`（:1722）、移动端"更多" `moreMask`（:1486）。
- 听力小测弹层 **连 `role="dialog"`/`aria-modal` 都没有**，只有 mask + X 关闭。
- 所有弹层都只靠"点 X / 点遮罩"关闭；**没有 `Escape` 键关闭、没有 Tab 焦点循环、关闭后没有把焦点还给触发元素**，且遮罩背后页面没有 `inert`/`aria-hidden`——键盘用户 Tab 仍可跑到底层页面。

**改法（通用 `openModal` 封装）**
```js
function openModal(html, {label='', onClose}={}){
  modalRoot.innerHTML = `<div class="modal-mask" id="mMask"><div class="modal" role="dialog" aria-modal="true" aria-label="${label}">${html}</div></div>`;
  const mask = $('#mMask'), dlg = $('.modal', modalRoot);
  const prev = document.activeElement;
  const focusables = () => $$('button,[href],input,select,textarea,[tabindex]:not([tabindex="-1"])', dlg).filter(el=>!el.disabled);
  (focusables()[0]||dlg).focus();
  mask.addEventListener('click', e=>{ if(e.target===mask) close(); });
  dlg.addEventListener('keydown', e=>{
    if(e.key==='Escape') close();
    if(e.key==='Tab'){ const f=focusables(); const i=f.indexOf(document.activeElement);
      e.preventDefault(); f[(i<0?0:i+f.length+(e.shiftKey?-1:1))%f.length].focus(); }
  });
  function close(){ modalRoot.innerHTML=''; prev && prev.focus(); onClose&&onClose(); document.removeEventListener('keydown', esc); }
  document.addEventListener('keydown', esc); function esc(e){ if(e.key==='Escape') close(); }
  return close;
}
```

### 2. Toast 没有 `aria-live` → 重要反馈对读屏用户不可见
**现状**：`toast(msg)` 把文字写进 `#toast`（app.js:21），但 `#toast` 在 HTML 里只是普通 `<div id="toast">`，不是 `aria-live`。像「无法访问麦克风」「打卡成功」「已收藏生词」这类关键状态，读屏用户完全收不到。

**改法**：`index.html` 改成
```html
<div id="toast" class="toast" role="status" aria-live="polite" aria-atomic="true"></div>
```

### 3. 所有"切换态"按钮缺 `aria-pressed` / `aria-current`
**现状**：导航项 `.nav-item`、声母/韵母/声调标签页 `.ph-tab`、词汇分类 `.cat-pills`、歌曲难度筛选、收藏 `favFilter`、待复习 `reviewBtn`、主题/语音开关——都只用 `.active` 类表达"开/选中"，读屏只念成普通按钮，念不出状态。

**改法**：切换时同步属性。
```js
function setActive(btn, on){ btn.classList.toggle('active', on); btn.setAttribute('aria-pressed', on?'true':'false'); }
// 导航/分页等表示"当前位置"的用 aria-current="page" 更好
```

### 4. 搜索框没有 `<label>` / `aria-label`
**现状**：`#phSearch`、`#vocabSearch`、`#songSearch`（index.html:147/190/225）只有 `placeholder`，没有关联 label。placeholder 在输入后会消失，且并非所有读屏都把它当标签念。

**改法**：要么加视觉隐藏 label，要么直接 `aria-label`：
```html
<input class="search" id="phSearch" aria-label="搜索音标或例字" placeholder="搜索音标或例字，如 aa / 山 / si…">
```

---

## P1 — 核心控件体验

### 5. 进度滑块 `input[type=range]` 零自定义样式
**现状**：`#spSeek`（唱歌页进度条，index.html:247）是原生 range，**CSS 里完全没有 `input[type=range]` / `::-webkit-slider-thumb` 样式**（已 grep 确认）。后果：移动端 thumb 又小又丑、命中区小、跨浏览器样式割裂；而且 `aria-valuetext` 没设，读屏只会报"百分比"而不是"01:23 / 03:45"。

**改法**：自定义 thumb/track + 时间文本：
```css
input[type=range]{ -webkit-appearance:none; appearance:none; height:6px; border-radius:999px; background:var(--line); }
input[type=range]::-webkit-slider-thumb{ -webkit-appearance:none; width:18px; height:18px; border-radius:50%; background:var(--red); box-shadow:0 1px 4px rgba(0,0,0,.3); }
input[type=range]::-moz-range-thumb{ width:18px; height:18px; border:none; border-radius:50%; background:var(--red); }
```
```js
spSeek.setAttribute('aria-valuetext', fmt(cur)+' / '+fmt(dur)); // 在 seek 事件里更新
```

### 6. 语速浮窗缺 Esc 关闭 + 方向键选择 + 角色
**现状**：`rateFloat`（index.html:337）有点击外部关闭 + `stopPropagation`（app.js:1773），但**没有 Esc 关闭、没有 `role`、选项不是 roving tabindex、打开后焦点不进入面板**。键盘用户能打开却不好关、也不好选。

**改法**：给面板加 `role="menu"` 或简单点——打开时把焦点移到第一个选项按钮，并绑 `Escape` 到 `panel.classList.add('hidden')`。

### 7. 全部 `<button>` 缺 `type="button"`（122 个，0 个有）
**现状**：grep 确认 `index.html` 63 个 + `app.js` 59 个 `<button>`，**带 `type=` 的 0 个**。当前页面没 `<form>`，暂不报错，但是隐患：一旦被包进 `<form>` 就会默认 `type="submit"` 触发提交；且语义不准确。

**改法**：批量补 `type="button"`（除真正的提交按钮外）。可用编辑器全量替换 `<button ` → `<button type="button" ` 后再微调。

### 8. 录音按钮状态仅用颜色表达
**现状**：`#ptRecord` 在 开始/停止/重新录音 间切换文字 + `.recording` 类（app.js:432-453），但 `.recording` 大概率只是变红，色盲用户 / 单色模式难分辨；也没有 `aria-pressed` 暴露"正在录音"给读屏。

**改法**：加一个闪烁的录制圆点（非纯颜色），并 `aria-pressed="true"`；或加 `aria-label` 随状态更新（"正在录音，点击停止"）。

---

## P2 — 打磨项

### 9. 异步 TTS 按钮缺 pending/disabled 态
**现状**：点「听示范」「全部朗读」会触发网络 fetch（`speak()`），但按钮在请求期间不禁用、无 spinner，连续点会重复请求 / 竞态。

**改法**：点击后置 `disabled` + 显示"…加载中"，请求结束/超时（如 8s）恢复。

### 10. 标签页未用 ARIA tabs 模式 + 无方向键
**现状**：`.ph-tab` / `.ph-tabs`（语音、文化）是按钮组，但没 `role="tablist"/"tab"/"tabpanel"/aria-selected`，也没有 ←/→ 键切换。

**改法**：补 `role` 并用方向键 roving focus；或至少保证 Tab 顺序合理 + `aria-selected`。

### 11. VU 音量条纯装饰无 aria
**现状**：`.vu-bar`（#vuBar）只有视觉，`#practiceCard` 没给读数屏的语义。低优先级——可加 `role="img" aria-label="实时音量"` 或忽略。

### 12. 搜索框无清除（Ⓧ）按钮
**现状**：三个搜索框输入后要手动全选删除。可加 `type="search"`（部分浏览器自带清除）或自定义 Ⓧ。

---

## 优先级路线图

| 优先级 | 控件 | 工作量 | 收益 |
|---|---|---|---|
| P0 | 模态框焦点陷阱 + Esc + `inert` | 中 | 键盘/读屏可用性的底线 |
| P0 | Toast `aria-live` | 极小 | 关键反馈对读屏可见 |
| P0 | 切换按钮 `aria-pressed`/`aria-current` | 小 | 全站状态可读 |
| P0 | 搜索框 `aria-label` | 极小 | 表单语义正确 |
| P1 | 进度滑块自定义样式 + 时间文本 | 小 | 移动端体验一致 |
| P1 | 语速浮窗 Esc/方向键/角色 | 小 | 键盘闭环 |
| P1 | `<button type="button">` 批量补 | 极小 | 消除隐患 |
| P1 | 录音按钮非颜色状态 | 小 | 色盲友好 |
| P2 | TTS 按钮 pending 态 | 小 | 防竞态 |
| P2 | 标签页 ARIA tabs 模式 | 小 | 键盘导航 |
| P2 | 搜索清除 / VU aria | 极小 | 打磨 |

> 最小投入最大回报：**P0-2（Toast aria-live）、P0-4（搜索 aria-label）、P1-7（type=button）** 都是"加一行属性"级别，建议先无脑做了；**P0-1 模态框封装** 一次写好能惠及所有弹层。
